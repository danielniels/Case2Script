fastapi==0.115.12
# bumped 0.34.2 -> 0.35.0: fastmcp requires uvicorn>=0.35
uvicorn[standard]==0.35.0
playwright==1.51.0
httpx==0.28.1
pandas==3.0.2
numpy==2.4.4
beautifulsoup4==4.13.4
# bumped 44.0.3 -> 45.0.1: fastmcp's joserfc dep requires cryptography>=45.0.1
cryptography==45.0.1
# >= not == : pinning the exact floor fastmcp needs (2.11.7) can resolve to
# an old pydantic-core release with no prebuilt Python 3.14 Windows wheel,
# forcing pip to compile Rust from source (needs a Rust+MSVC toolchain most
# machines don't have). Letting pip pick the newest pydantic gets a
# pydantic-core build that already ships a cp314 wheel. Same reasoning for
# Pillow/lxml below — those exact pins predate their cp314 wheel builds.
pydantic>=2.11.7
requests==2.32.3
Pillow>=11.2.1
lxml>=5.4.0
python-multipart==0.0.20
starlette==0.46.2
openpyxl==3.1.2
# bumped 1.0.1 -> 1.1.0: fastmcp requires python-dotenv>=1.1.0
python-dotenv==1.1.0
aiosqlite==0.20.0
# fastmcp>=3.0 requires starlette>=1.0.1, which would force a fastapi bump
# too (conflicts with fastapi==0.115.12/starlette==0.46.2 pinned above).
# 2.14.7 is the newest release that installs clean against the existing
# pins — verified locally before choosing this version.
fastmcp==2.14.7
